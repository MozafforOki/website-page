import React from 'react';
import Nav from '../Nav/Nav';

const TopBar = () => {
    return (
        <div>
            <header>
                <Nav></Nav>
            </header>
        </div>
    );
};

export default TopBar;